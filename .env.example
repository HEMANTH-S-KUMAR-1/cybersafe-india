# Azure Translator Configuration
# To enable real-time translation, set up Azure Translator and configure these variables:

# Your Azure Translator subscription key
VITE_AZURE_TRANSLATOR_KEY=your_subscription_key_here

# Your Azure Translator endpoint (optional, defaults to global endpoint)
VITE_AZURE_TRANSLATOR_ENDPOINT=https://api.cognitive.microsofttranslator.com

# Your Azure Translator region (optional, defaults to 'global')
VITE_AZURE_TRANSLATOR_REGION=global

# Instructions:
# 1. Create an Azure Translator resource in Azure Portal
# 2. Copy your subscription key from the resource's "Keys and Endpoint" section
# 3. Set the above variables in your .env file
# 4. Real-time translation will be automatically enabled when valid credentials are provided

# Note: Without these credentials, the app will fall back to static translations only
